import { Navbar as BSNavbar, Nav} from "react-bootstrap";
import { LinkContainer } from "react-router-bootstrap";

const Navbar = () => (
  <BSNavbar>
    <LinkContainer>
      <BSNavbar.Brand>
        test
      </BSNavbar.Brand>>
    </LinkContainer>
  </BSNavbar>
)